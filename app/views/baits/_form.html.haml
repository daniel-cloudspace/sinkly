= form_for @admin_bait do |f|
  -if @admin_bait.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_bait.errors.count, "error")} prohibited this admin_bait from being saved:"
      %ul
        - @admin_bait.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
